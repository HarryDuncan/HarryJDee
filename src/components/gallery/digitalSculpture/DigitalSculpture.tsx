import React from 'react';
import {connect} from "react-redux";
import './../galleryStyles.scss';



interface IDigitalSculptureProps {
	

}
const DigitalSculpture: React.FunctionComponent<IDigitalSculptureProps> = props => { 
	
	return(
			<div/>

			)
} 

const mapStateToProps = (state: any) => ({
 
})
 
const mapDispatchToProps = {
	
};

export default connect(mapStateToProps, mapDispatchToProps)(DigitalSculpture);
